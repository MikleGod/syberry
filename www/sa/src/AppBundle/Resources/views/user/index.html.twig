{% extends '@App/base.html.twig' %}
{% block body %}
    <div id="user-body">
        <form action="{{ url('user_new') }}" method="post"><button type="submit" class="btn btn-primary">New</button></form>
        <table class="table table-striped">
            <thead class="thead-dark">
            <tr>
                <th scope="col">{% trans %}id{% endtrans %}</th>
                <th scope="col">{% trans %}name{% endtrans %}</th>
                <th scope="col">{% trans %}surname{% endtrans %}</th>
                <th scope="col">{% trans %}email{% endtrans %}</th>
                <th scope="col">{% trans %}nickname{% endtrans %}</th>
                <th scope="col">{% trans %}actions{% endtrans %}</th>
            </tr>
            </thead>
            <tbody>
            {% for user in users %}
                <tr>
                    <th scope="row">{{ user.id }}</th>
                    <td><a href="{{ url('user_show', {id: user.id}) }}">{{ user.firstName }}</a></td>
                    <td>{{ user.surname }}</td>
                    <td>{{ user.email }}</td>
                    <td>{{ user.nickname }}</td>
                    <td>
                        <div class="btn-group" role="group" aria-label="Basic example">
                            <a href="{{ url('user_edit', {id: user.id}) }}"><button type="button" class="btn btn-secondary">{% trans %}edit{% endtrans %}</button></a>
                            <form method="post" action="{{ url('user_delete', {id: user.id}) }}">
                                <button type="submit" class="btn btn-secondary">{% trans %}delete{% endtrans %}</button>
                            </form>
                        </div>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}